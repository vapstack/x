## stack 

A basic stack trace that can be encoded/decoded or sent over wire.

```go
frames := stack.Trace(32, 1) // (max, skip)
```

See [GoDoc](https://pkg.go.dev/github.com/vapstack/x/stack) for reference
